package com.aric3435.temporaltrials.client;

/**
 * Client-side MusicController stub.
 * TemporalTrialsClient and ClientInit call MusicController.register() at startup;
 * implement client-side audio or receivers here later (loop_state network handler, etc).
 */
public final class MusicController {
    private MusicController() {}

    public static void register() {
        // No-op for now. Expand with client receivers, local playback, per-player volume,
        // or custom music UI as needed.
    }
}